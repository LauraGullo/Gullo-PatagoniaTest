
spring:
  application:
    name: loan-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      fetch-registry: true
      register-with-eureka: true