server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/LauraGullo/Prueba-configServer.git
          search-paths: config-data/client-service

  application:
    name: config-service
